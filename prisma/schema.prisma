generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"   // ← MySQLなら "mysql" などに変更
  url      = env("DATABASE_URL")
}

model Post {
  id        Int      @id @default(autoincrement())
  title     String
  postedAt  DateTime
  content   String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}